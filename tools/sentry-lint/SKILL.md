---
name: sentry-lint
description: >
  [已归档] 静态检查功能已合并进 sentry-static（v8.0）。
  说"lint xxx"或"检查结构"会自动路由到 sentry-static。
---

# sentry-lint · 已归档

此工具已合并进 `sentry-static`（v8.0），作为静态检查兼容入口。

**使用方式**：说 `lint xxx` / `检查结构` / `check xxx` → 自动触发 `sentry-static`。

*归档于 v7.0 · 2026-04-27*
